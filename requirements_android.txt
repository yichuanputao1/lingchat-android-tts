setuptools
wheel

# Core dependencies
torch>=2.2.0
numpy>=1.21.0
scipy>=1.7.0

# Text processing and NLP
transformers>=4.21.0
tokenizers>=0.13.0
# pyopenjtalk is optional on Android; Japanese TTS will be limited if unavailable
# pyopenjtalk>=0.3.3

# Audio processing
pyworld>=0.3.0
librosa>=0.9.0
soundfile>=0.12.0

# ONNX support (CPU-only)
onnx>=1.14.0
onnxruntime>=1.15.0

# Web server
fastapi>=0.95.0
uvicorn>=0.21.0
python-multipart>=0.0.6

# System monitoring (GPUtil removed - NVIDIA only, not compatible with Android)
psutil>=5.9.0

# Additional utilities
pydantic>=1.10.0
tqdm>=4.64.0
pyyaml>=6.0

loguru
accelerate
num2words

# ==== Termux Installation Notes ====
# Install PyTorch (CPU-only) on Termux:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu
#
# If pyopenjtalk compilation fails, consider:
#   pip install pyopenjtalk --no-build-isolation
# or skip it (server will run without Japanese support)
